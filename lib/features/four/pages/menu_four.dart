import 'package:flutter/material.dart';

class MenuFour extends StatelessWidget {
  const MenuFour({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("4", style: TextStyle(fontSize: 50)));
  }
}
