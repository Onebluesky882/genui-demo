import 'package:flutter/material.dart';

class MenuThree extends StatelessWidget {
  const MenuThree({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("3", style: TextStyle(fontSize: 50)));
  }
}
